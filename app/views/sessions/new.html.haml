%h1 Sign in
= form_for(:session, url: sessions_path) do |f|
  .field
    .label= f.label :email
    .input= f.text_field :email
  .field
    .label= f.label :password
    .input= f.password_field :password
  .actions= f.submit "Sign in"
%p New user? #{ link_to "Sign up now!", signup_path }
